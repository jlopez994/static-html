# Proyecto para generar web estática con SCSS

## Local

Usa `npm i` para instalar las dependencias y `npm start` para lanzar el servidor en modo desarrollo

## Producción

Usa `npm run build` para obtener el código fuente de la web